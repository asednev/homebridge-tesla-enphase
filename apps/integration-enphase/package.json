{
  "name": "integration-enphase",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "tsc --watch",
    "build": "rimraf ./dist && tsc",
    "dev": "rimraf ./dist && tsc && node dist/debug.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexey Sednev",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.1.3",
    "core": "../core",
    "typescript": "^4.8.4"
  },
  "devDependencies": {
    "@types/node": "^18.11.0",
    "rimraf": "^3.0.2"
  }
}
